# frozen_string_literal: true

RSpec.describe Rubocop::Neeto do
  it "has a version number" do
    expect(Rubocop::Neeto::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
